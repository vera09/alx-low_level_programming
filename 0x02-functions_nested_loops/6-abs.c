#include "main.h"
/**
 * _abs - prints the sign of a number
 * @n: number to print
 * Return: the absolute value of the n
 */
int _abs(int n)
{
	if(n >= 0)
	{
	return(n);
	}
	else
	{
	return (-n);
	}
}
